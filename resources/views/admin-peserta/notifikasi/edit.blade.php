@extends('layouts.portal')
@section('content')<h1 class="text-2xl font-bold">Ubah Notifikasi</h1><form method="POST" action="{{ route('admin-peserta.notifikasi.update', $notifikasi) }}" class="mt-4 space-y-3">@csrf @method('PUT')<input class="border p-2" name="user_id" value="{{ $notifikasi->user_id }}"><input class="border p-2" name="judul" value="{{ $notifikasi->judul }}"><textarea class="block border p-2" name="pesan">{{ $notifikasi->pesan }}</textarea><select name="kategori"><option value="{{ $notifikasi->kategori }}">{{ $notifikasi->kategori }}</option></select><select name="tipe"><option value="{{ $notifikasi->tipe }}">{{ $notifikasi->tipe }}</option></select><button class="bg-cyan-800 p-2 text-white">Simpan</button></form>@endsection
